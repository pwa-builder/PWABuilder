import {
  codeFormat
} from '../../../helpers/code-format';
import { module, test } from 'qunit';

module('CodeFormatHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = codeFormat(42);
  assert.ok(result);
});
