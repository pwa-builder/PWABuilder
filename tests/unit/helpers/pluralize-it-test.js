import {
  pluralizeIt
} from '../../../helpers/pluralize-it';
import { module, test } from 'qunit';

module('PluralizeItHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = pluralizeIt(42);
  assert.ok(result);
});
