import {
  issueNotifications
} from '../../../helpers/issue-notifications';
import { module, test } from 'qunit';

module('IssueNotificationsHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = issueNotifications(42);
  assert.ok(result);
});
