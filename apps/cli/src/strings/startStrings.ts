// Descriptions

export const startDescriptions = {
  viteArgsDescription:  "Arguments to pass directly to the Vite build process.",
  commandDescription: "Run the PWA Starter on a Vite dev server."
}

// Errors

export const startErrors = {
  invalidDirectory: `Cannot execute start because the current working directory is not a valid PWA Starter template. 
    Make sure you are executing the start command from a PWA Starter template directory.`,
}
