// Descriptions

export const deployDescriptions = {
  commandDescription: "Deploy the PWA Starter to Azure Static Web Apps.",
}

// Errors

export const deployErrors = {
  invalidDirectory: `Cannot execute deploy because the current working directory is not a valid PWA Starter template. 
    Make sure you are executing the deploy command from a PWA Starter template directory.`,
}