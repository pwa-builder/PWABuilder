{{input value=logoUrl type="url" placeholder="URL for logo"}}
<button {{action 'addLogo'}}>+</button>

<div class="logos">
  {{#each logo in logos}}
    <div>src: {{logo.src}} sizes: {{logo.sizes}}</div>
    <button {{action 'removeLogo' logo}}>-</button>
  {{/each}}
</div>

