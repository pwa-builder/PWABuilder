{
  "name": "@pwabuilder/manifest-validation",
  "version": "0.0.1",
  "description": "",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "./dist"
  ],
  "scripts": {
    "test": "rimraf ./dist && tsc --p ./tsconfig.test.json && node --test",
    "build": "rimraf ./dist && tsc --p ./tsconfig.json",
    "build:watch": "tsc --watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.6.3"
  },
  "dependencies": {
    "node-fetch": "^3.3.0"
  }
}
