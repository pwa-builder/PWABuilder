{
  "name": "@pwabuilder/manifest-validation",
  "version": "0.0.1",
  "description": "",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "./dist"
  ],
  "scripts": {
    "test": "npx rimraf ./dist && tsc --p ./tsconfig.test.json && node --test",
    "build": "npx rimraf ./dist && tsc --p ./tsconfig.json"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.6.3"
  }
}
