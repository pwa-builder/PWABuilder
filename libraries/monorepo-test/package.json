{
  "name": "@pwabuilder/monorepo-test",
  "version": "0.0.1",
  "module": "./dist/index.js",
  "main": "./dist/index.js",
  "scripts": {
    "clean": "rimraf build/ && rimraf dist/",
    "build": "npm run clean && tsc",
    "test2": "echo should run first"
  },
  "dependencies": {
    "browser-fs-access": "^0.23.0"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.6.3"
  }
}
