machine:
  node:
    version: 0.12.2
dependencies:
  override:
    - npm install
    - bower install
    - npm install ember-cli -g
test:
  override:
    - ember test
deployment:
  staging:
    branch: deployment
    commands:
      - ./build/push.sh
