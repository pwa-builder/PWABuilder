machine:
  node:
    version: 0.10.36
dependencies:
  override:
    - npm install ember-cli -g
    - npm install bower -g
    - npm install phantomjs -g
    - npm install
    - bower install
test:
  override:
    - ember test
deployment:
  staging:
    branch: deployment
    commands:
      - ./build/push.sh
