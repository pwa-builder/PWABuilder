{
  "name": "project",
  "version": "0.0.1",
  "workspaces": [
    "./packages/*"
  ],
  "description": "Helping developers build better PWAs ",
  "main": "index.js",
  "private": true,
  "scripts": {
    "build": "npm run build -ws",
    "dev": "npm run build && npm run dev -w pwabuilder"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "typescript": "^4.3.2"
  }
}
